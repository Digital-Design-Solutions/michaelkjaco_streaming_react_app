import React from "react";

export const SliderDots = (uniqueKey, color) => {
  return (
    <div
      style={{
        backgroundColor: color,
        height: "12px",
        width: "12px",
        borderRadius: "50px",
        margin: "40px 4px",
      }}
    />
  );
};
